DATABASE_URL=postgresql+asyncpg://usuario:password@host:puerto/nomina
SECRET_KEY=tu_secret_key_aleatorio_aqui
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Webhook n8n para notificaciones de eventos (dejar vacío para deshabilitar)
WEBHOOK_N8N_EVENTOS_URL=https://tu-n8n.dominio.com/webhook/nuevo-evento

# API key para integraciones externas (n8n → FastAPI)
# Generá una con: python -c "import secrets; print(secrets.token_hex(32))"
INTEGRATION_API_KEY=cambia_esto_por_una_clave_segura
